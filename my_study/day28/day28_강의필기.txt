지역클래스

메모리 영역 중 스택 영역에 존재하기 때문에
메서드 호출~ 종료까지 메모리에존재하는 영역
---------------------------------------------

내부 클래스의 객체를 생성하는 방법

외부클래스.내부클래스명 객체명 = new 외부클래스생성자().내부클래스생성자( );
			        외부클래스객체.내부클래스생성자( );	